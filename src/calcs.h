#ifndef CALCS_H
#define CALCS_H

#include <math.h>

double area_coroa(double, double);

double area_circunferencia(double);

double comprimemnto_circunferencia(double);

double area_setor(double, double);

double reta(double, double, double );

double coef(double, double, double, double );

double magnitude(double m, int option, double line, double raio);

double Min(double , double);

double AreaCircle(double, double );

#endif // CALCS_H
